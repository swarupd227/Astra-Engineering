/**
 * Feature flag: use LangGraph for stack modernization workflow (traceability, backtracking).
 * Re-exports from central stack-modernization config.
 */
export { useLangGraphStackModernization } from "../config";
