**Artifacts Generation Implementation Summary**

- **Purpose:** Describe how workflow artifacts (Epics, Features, User Stories) are generated by the backend, and list the exact prompt files, adapter files, and code locations involved.

- **Primary code path used at runtime:**
  - **File:** `server/ai-service.ts` — function `generateAgileArtifacts`
  - **Definition location:** starts at line ~2233 (see file lines `2228-2920` for the full message construction and parsing logic).
  - **How it's used:** `generateAgileArtifacts(requirement, ...)` builds large, production-grade `system` + `user` messages inline and calls `openai.chat.completions.create(...)` to request a JSON response with `epics`, `features`, and `userStories`.
  - **Key behaviors implemented here:**
    - Builds `complianceSection`, `backlogSection`, and `personaSection` (lines ~2240–2600) that are injected into the system prompt when applicable.
    - Constructs a strict set of quality rules and an expected JSON schema for epics/features/stories inside the `messages` array (lines ~2520–2920).
    - Uses `response_format: { type: "json_object" }` and parses `response.choices[0].message.content` as JSON, then validates relationships and required fields (epic/feature/story ids, links) (lines ~2760–2860).

- **Prompt files / adapters present in repository (not the runtime source used by `generateAgileArtifacts`):**
  - `server/prompts/prompt_workflow_requirements.ts`
    - Exports `promptWorkflowRequirements` which includes a concise user-level template intended to return a compact JSON-only artifact set (file lines ~1–39).
    - Example content: short template with generation rules (epics/features/stories, concise ACs, token-efficiency constraints).
  - `server/prompts/workflow_artifacts_user.ts` (adapter)
    - Re-exports user prompt: `export const workflowArtifactsUser: string = promptWorkflowRequirements.content;` (file lines ~1–6).
  - `server/prompts/workflow_artifacts_system.ts` (adapter)
    - Re-exports a system-level prompt: `export const workflowArtifactsSystem: string = workflowUserstoryFormatInstructions.content;` (file lines ~1–6).

- **Where prompts are imported in the AI service:**
  - At the top of `server/ai-service.ts` the adapters are imported:
    - `import { workflowArtifactsSystem } from "./prompts/workflow_artifacts_system";` (line ~6)
    - `import { workflowArtifactsUser } from "./prompts/workflow_artifacts_user";` (line ~7)
  - Note: these imports exist and the adapter modules simply re-export prompt strings, but the active artifact-generation call in `generateAgileArtifacts` does not reference `workflowArtifactsUser` / `workflowArtifactsSystem` in the `messages` array — instead it constructs its own detailed inline system + user prompts (see next section).

- **Exact `generateAgileArtifacts` responsibilities (high level):**
  - Inspect `requirement` input + optional `complianceGuidelines`, `backlogContext`, and `selectedPersonaIds` (lines ~2233–2270).
  - Build contextual sections (compliance/backlog/personas) and append them to the system prompt (lines ~2270–2600).
  - Provide strict production-grade quality standards (user story format, acceptance criteria requirements, subtask format, testability constraints) inside the system message (lines ~2520–2760).
  - Send a chat completion request with deterministic settings (`temperature: 0.2`, `max_tokens: 32768`, `response_format: { type: 'json_object' }`) and expect a JSON object with keys: `epics`, `features`, `userStories`, `personas` (lines ~2680–2760).
  - Parse and validate the returned JSON, enforce minimal counts and field presence, and throw descriptive errors if validation fails (lines ~2760–2860).

- **Why there are two prompt locations:**
  - `prompt_workflow_requirements.ts` provides a compact, maintainable user-role prompt (short JSON-only template) intended as a reusable prompt artifact.
  - `ai-service.ts` contains the production prompt as an inline, highly-detailed system + user instruction set — this is the prompt actually used at runtime by `generateAgileArtifacts`.

- **Files & code excerpts (quick reference)**
  - `server/ai-service.ts`
    - Imports: lines 1–12 (includes `workflowArtifactsSystem` & `workflowArtifactsUser`).
    - Function: `generateAgileArtifacts` starts at line 2233. Key ranges:
      - Context sections (compliance/backlog/persona): lines ~2240–2600
      - Messages & prompt construction: lines ~2520–2920
      - Response parsing & validation: lines ~2760–2860
  - `server/prompts/prompt_workflow_requirements.ts`
    - File content with concise rules and `promptWorkflowRequirements` export — file lines ~1–39.
  - `server/prompts/workflow_artifacts_user.ts`
    - Re-export line: `export const workflowArtifactsUser: string = promptWorkflowRequirements.content;` — file lines ~1–6.
  - `server/prompts/workflow_artifacts_system.ts`
    - Re-export line: `export const workflowArtifactsSystem: string = workflowUserstoryFormatInstructions.content;` — file lines ~1–6.

- **Recommendation / Next steps (options you can pick):**
  - Option A — Centralize prompts: modify `generateAgileArtifacts` to use the adapter prompt strings (`workflowArtifactsSystem` and `workflowArtifactsUser`) instead of the long inline prompt. This makes maintenance easier and keeps prompt content in the `server/prompts` folder.
  - Option B — Keep the inline production prompt but update `server/prompts/prompt_workflow_requirements.ts` to match it, then explicitly import and use that adapter in `generateAgileArtifacts` so the file serves as the source of truth.
  - Option C — Leave runtime behavior unchanged (current production inline prompt) and treat `prompt_workflow_requirements.ts` as a legacy/alternate template.

- **If you want I can:**
  - Implement Option A and update `generateAgileArtifacts` to reference the prompts in `server/prompts/*` (I can patch `server/ai-service.ts` and run a quick static check), or
  - Implement Option B and sync the short prompt file with the inline prompt.

---
Generated on: 2025-12-10
